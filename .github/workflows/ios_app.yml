name: Build, Test and Deploy the YATT iOS App

on:
  push:
    branches: 
      - main
    paths:
      - 'yattt_ios/**'
  pull_request:
    branches: 
      - main
    paths:
      - 'yattt_ios/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    runs-on: macos-latest
        
    steps:
      - uses: actions/checkout@v4
      
      - name: Debug - List All Files in Repository
        run: |
          echo "Repository Files:"
          ls -R
      - name: Debug - Verify xcodeproj Path
        run: |
          echo "Listing files in yattt_ios directory:"
          ls -R yattt_ios
          echo "Checking if yattt_ios_app.xcodeproj exists:"
          if [ -e yattt_ios/yattt_ios_app.xcodeproj ]; then
            echo "Found yattt_ios_app.xcodeproj"
          else
            echo "Error: yattt_ios_app.xcodeproj not found"
            exit 1
          fi

            
      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_15.0.app/Contents/Developer

      - name: Run Unit Tests with Coverage
        run: |
          xcodebuild test \
            -project yattt_ios_app.xcodeproj \
            -scheme yattt_ios_app \
            -sdk iphonesimulator \
            -destination "platform=iOS Simulator,name=iPhone 15,OS=latest" \
            -enableCodeCoverage YES
            
      - name: Upload Code Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            ~/Library/Developer/Xcode/DerivedData/**/Logs/Test/*.xcresult

  build:
    runs-on: macos-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_15.0.app/Contents/Developer

      - name: Build App
        run: |
          xcodebuild test \
            -project yattt_ios_app.xcodeproj \
            -scheme yattt_ios_app \
            -sdk iphonesimulator \
            -destination "platform=iOS Simulator,name=iPhone 16,OS=latest" \
            -enableCodeCoverage YES

            -sdk iphoneos \
            -configuration Release \
            -archivePath $PWD/build/yattt_ios_app.xcarchive \
            archive
